generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model DevRequestImage {
  id        String   @id @default(cuid())
  data      String   // base64 image data
  mimeType  String
  createdAt DateTime @default(now())
}
